xmonad
======

xmonad config files

dependencies:

* dzen2
* terminator

installation:

```
$ git clone git@github.com:Chavao/xmonad.git
$ ln -sf xmonad ~/.xmonad
$ xmonad --recompile
$ xmonad --restart
```

preview:

<img src="http://i.imgur.com/yD8w1he.png" />
