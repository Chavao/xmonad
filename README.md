xmonad
======

xmonad config files

dependencies:

* dzen2
* terminator

installation:

```
$ git clone git@github.com:Chavao/xmonad.git
$ ln -sf xmonad ~/.xmonad
$ xmonad --recompile
$ xmonad --restart
```
